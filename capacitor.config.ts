import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Generic Test Project',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
